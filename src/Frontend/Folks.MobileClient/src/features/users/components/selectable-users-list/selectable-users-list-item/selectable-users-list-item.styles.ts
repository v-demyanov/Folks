import { StyleSheet } from 'react-native';

const buildStyles = () =>
  StyleSheet.create({
    listItem: {
      paddingLeft: 10,
    },
    title: {
      fontWeight: 'bold',
    },
  });

export default buildStyles;
