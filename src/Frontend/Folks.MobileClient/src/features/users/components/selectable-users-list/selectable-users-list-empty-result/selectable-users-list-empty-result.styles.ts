import { StyleSheet } from 'react-native';

const buildStyles = () =>
  StyleSheet.create({
    centeredView: {
      display: 'flex',
      paddingTop: '60%',
      alignItems: 'center',
    },
  });

export default buildStyles;
