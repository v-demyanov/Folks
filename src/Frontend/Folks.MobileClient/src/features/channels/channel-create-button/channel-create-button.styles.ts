import { StyleSheet } from 'react-native';

const buildStyles = () =>
  StyleSheet.create({
    button: {
      position: 'absolute',
      right: 0,
      bottom: 0,
      marginBottom: 20,
      marginRight: 20,
    },
  });

export default buildStyles;
