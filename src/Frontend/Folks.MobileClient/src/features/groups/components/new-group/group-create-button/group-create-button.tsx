import { FAB } from 'react-native-paper';

const GroupCreateButton = (): JSX.Element => {
  return (
    <FAB
      icon="check"
      style={{ position: 'absolute', bottom: 16, right: 16 }}
      onPress={() => {}}
    />
  );
};

export default GroupCreateButton;
